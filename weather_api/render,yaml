services:
  - type: web
    name: weather-api-ui
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_OPENWEATHER_API_KEY
        sync: false
